
server:
  ingress:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
    hosts:
      - argocd.rcomanne.nl

configs:
  cm:
    url: argocd.rcomanne.nl

logging:
  format: json

